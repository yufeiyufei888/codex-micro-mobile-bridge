# Codex Micro V1.0.6 更新说明

发布日期：2026-08-12

## 本次修复

- 修复 V1.0.5 无法同步任何新消息和回复的严重回归。
- 修复 Windows 上 Codex 正在写入会话文件时，Bridge 普通读取首行产生共享冲突，并把正确主会话误判为非主会话的问题。
- 会话元数据、消息尾部现在统一采用 `FileShare.ReadWrite | FileShare.Delete` 安全只读访问。
- 手机消息发送后等待精确会话匹配期间，继续监听已经验证的当前会话，不再暂停全部消息同步。
- 发送手机消息时不再提前清除正确的会话绑定和文件游标。
- 电脑状态与手机状态统一：只有 Codex 输入框和当前会话文件均可用时才显示“桌面同步可用”。

## 保留功能

- 手机向当前桌面 Codex 对话直接发送消息。
- 电脑直接输入的新消息及 Codex 回复自动同步到手机。
- 完整对话历史、最近回复、未读状态与去重。
- Computer Use 和普通桌面审批的手机长按批准、拒绝与一次性绑定校验。
- 局域网 WSS、证书指纹固定、设备密钥认证和前台服务后台保活。

## V1.0.6 重点验收

1. 手机发送一句唯一消息，确认手机无需再次发送即可收到本轮回复。
2. 电脑输入一句唯一消息，确认手机无需刷新即可同时出现该消息及回复。
3. 触发 Computer Use 记事本审批，确认手机批准后只启动一次，主页退出待审批。

V1.0.5 因消息同步链完全失效，不建议继续使用。
