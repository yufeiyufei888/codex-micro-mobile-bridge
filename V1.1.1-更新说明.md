# Codex Micro V1.1.1 更新说明

V1.1.1 是 V1.1.0 多对话候选版的桌面状态热修复，重点处理目标模式、计划模式和计划提问导致的错误“电脑服务降级”。

## 本次修复

- 将“Codex 窗口在线”和“普通输入框当前可写”拆分为两个判断。
- 打开目标、计划模式面板，或计划提问临时替换普通输入框时，WSS 和桌面窗口仍保持在线，不再误报电脑服务降级。
- 上述交互期间手机发送会得到明确的“普通输入框暂不可用”提示，不会误发到模式面板、选项或其他软件。
- 当前桌面对话优先使用可见标题与 `.codex/session_index.jsonl` 的最新会话名称精确匹配。
- 其他后台任务继续写入会话文件时，不再按“最近写入文件”抢走手机当前对话的绑定。
- Codex Chromium 控件树在插入目标、计划面板时短暂重建，不再等同于整个桌面服务离线。
- 目标/计划提问仍需在电脑端选择；本热修复不会把计划选项伪装成权限审批，也不会替用户自动选择。

## 自动验证

- Windows Bridge：85/85 测试通过。
- 当前真实 Codex 窗口只读识别测试通过；测试不执行点击、输入或审批。
- Android 仅提升版本号以保持电脑和手机版本一致，业务代码未改动。

## 仍需实机验收

1. 在普通对话、目标模式和计划模式之间切换，手机始终保持网络已连接。
2. 计划提问选项出现时，手机不再显示电脑服务降级。
3. 切换两个正在运行的 Codex 对话，手机当前对话不被后台任务的文件写入错误抢占。
4. 返回普通输入框后，手机发送、实时回复与 Computer Use 审批继续正常。

完成上述手机与电脑联合测试前，V1.1.1 仍是候选热修复，不宣称实机验收完成。
