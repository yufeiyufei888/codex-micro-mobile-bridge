# Codex Micro V1.1.0 更新说明

V1.1.0 是多对话隔离升级版，基于 V1.0.6 已通过实机验证的实时回复、后台连接和 Computer Use 审批链路继续开发。

## 主要更新

- 每个 Codex root rollout 使用独立稳定 threadId，移除所有桌面对话共用 `desktop-current` 的合并逻辑。
- 手机首页第一张卡固定为当前可操作对话，下面显示最多五个最近对话。
- 回复历史、完成未读、运行状态和审批请求按对话隔离；子代理 rollout 不进入对话列表。
- 历史对话为只读，避免把手机消息误发到电脑当前打开的另一个对话。
- 审批中心明确显示所属对话，并用“操作、目标、权限、影响”呈现请求；工作目录默认折叠。
- 审批响应额外校验请求所属对话仍为当前桌面对话，切换对话后旧审批会失败关闭。

## 自动验证范围

- Windows 构建、单元测试和桥接协议 fixtures。
- Android JVM 单元测试、Debug APK 构建、包名、版本号和签名连续性。
- 多 root 会话排序、稳定 ID、排除 subagent、历史对话只读和审批摘要格式。

## 仍需实机验收

- 在电脑打开两个以上 Codex 对话，确认手机分别显示标题、状态、回复和历史。
- 切换电脑当前对话，确认手机首卡随之更新，历史对话不会收到错误回复。
- 两个对话分别触发审批，确认手机能辨认所属对话，批准后只影响目标对话。
- 从 V1.0.6 覆盖安装 V1.1.0，确认配对和已有历史保留。

在用户完成上述手机与电脑联合测试前，V1.1.0 应视为候选版，不宣称实机验收完成。
